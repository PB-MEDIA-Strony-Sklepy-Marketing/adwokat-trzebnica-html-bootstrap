# Package installation settings
save-exact=true
package-lock=true
engine-strict=true

# Security settings
audit-level=moderate
fund=false

# Performance settings
prefer-offline=false
progress=true

# Development settings
loglevel=warn

# Registry settings (default npm registry)
registry=https://registry.npmjs.org/

# Cache settings
cache=/tmp/npm-cache
